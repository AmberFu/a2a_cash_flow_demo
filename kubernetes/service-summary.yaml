apiVersion: v1
kind: Service
metadata:
  name: summary-agent-service
  namespace: a2a-demo
spec:
  selector:
    app: summary-agent
  ports:
    - protocol: TCP
      port: 50003       # 🚨 Service 提供的 Port
      targetPort: 50003 # 🚨 容器實際運行的 Port
  type: ClusterIP

