apiVersion: v1
kind: Service
metadata:
  name: remote-agent-2-service
  namespace: a2a-demo
spec:
  selector:
    app: remote-agent-2
  ports:
    - protocol: TCP
      port: 50002       # 🚨 Service 提供的 Port
      targetPort: 50002 # 🚨 容器實際運行的 Port
  type: ClusterIP