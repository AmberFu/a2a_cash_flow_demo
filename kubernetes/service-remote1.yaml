apiVersion: v1
kind: Service
metadata:
  name: remote-agent-1-service
  namespace: a2a-demo
spec:
  selector:
    app: remote-agent-1
  ports:
    - protocol: TCP
      port: 50001       # 🚨 Service 提供的 Port
      targetPort: 50001 # 🚨 容器實際運行的 Port
  type: ClusterIP